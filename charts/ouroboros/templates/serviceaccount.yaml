apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ouroboros.serviceAccountName" . }}
  labels:
    {{- include "ouroboros.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
